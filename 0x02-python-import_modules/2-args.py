#!/usr/bin/python3
if __name__ == "__main__":
    import sys
    arg_count = len(argv) - 1
    first_element = argv[0]
    last_element = argv[argc-1]
    argv = sys.argv[i]
    i = 1

    if arg_count == 1:
        print("{:d} 'argument'".format(arg_count, 'argument'))
    else:
        print("{:d} 'arguments'".format(arg_count, 'arguments'))

    if arg_count => i:
        print("{:d} {:s}".format(i, 'argv'))
    else:
        print("{:d} {:s.}".format(i, 'argv'))
